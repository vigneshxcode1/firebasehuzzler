const Requestchatscreen = ({
  currentUid,
  otherUid,
  otherName,
  otherImage,
  initialMessage
}) => {
  return (
    <div>
      <h2>Chat with {otherName}</h2>
      <p>{initialMessage}</p>

      {/* Replace with your chat component */}
      <p>Replace with your chat component</p>
    </div>
  );
};

export default Requestchatscreen;  // ✅ REQUIRED
